---
name: prompt-optimizer
description: 将用户的模糊提示词（prompt）优化为结构清晰、逻辑严密、易于 Claude 理解和执行的结构化提示词。适用场景：用户说"帮我优化这个提示词"、"这个 prompt 效果不好"、"帮我把这段需求变成 prompt"、"让 Claude 更好地理解我的意图"，或者直接粘贴一段模糊的自然语言需求并隐含希望变成 prompt 的意图。即使用户没有明确说"优化 prompt"，只要他们提供了一段描述性需求并期待 Claude 按某种方式执行复杂任务，也应触发此 skill。
---

# Prompt Optimizer Skill

将用户的模糊、口语化提示词，转化为结构清晰、角色明确、任务分层、输出规范的高质量提示词。

---

## 核心原则

一个好的提示词应该回答以下五个问题：

1. **Who**：Claude 扮演什么角色？具备什么专业背景？
2. **What**：核心任务是什么？拆解成哪些子任务？
3. **How**：执行顺序是什么？每步的输出格式是什么？
4. **Constraint**：有哪些限制条件、边界、不能做的事？
5. **Interaction**：遇到歧义时怎么办？如何与用户协作？

---

## 执行流程

### Step 1：诊断原始提示词

阅读用户提供的原始提示词，识别以下问题（内心分析，不必全部输出给用户）：

- **角色模糊**：只说"你是专家"，没有说明专业边界和知识背景
- **目标笼统**：用"完善""优化""改进"等动词，但没说完善什么、优化到什么程度
- **任务未分层**：多个任务堆在一起，没有优先级和执行顺序
- **输出格式缺失**：没有说明期望的输出形式（文字/图表/代码/文档等）
- **协作方式不明**：没有说明遇到歧义时是自行假设还是主动提问
- **约束条件缺失**：没有说明边界条件、使用场景、受众等

### Step 2：补全关键信息（逐题问答）

如果原始提示词中缺少关键维度，**逐个提问而非一次性全部抛出，也不自行假设**。

#### 2.1 问题优先级排序

在诊断阶段（内心）识别出所有缺失维度后，按以下优先级排序，依次提问：

| 优先级 | 维度 | 示例问题 | 示例选项 |
|--------|------|---------|---------|
| P1（必问） | 任务范围/核心目标 | 这个任务最终要产出什么？ | ①分析报告 ②可执行方案 ③PRD文档 ④其他（请说明） |
| P1（必问） | 输出格式 | 期望输出什么格式？ | ①Markdown ②Word文档 ③代码 ④表格 ⑤其他 |
| P2（影响结构） | 目标受众 | 输出内容给谁用？ | ①产品/研发团队 ②业务/决策层 ③数据团队 ④甲方/客户 |
| P2（影响结构） | 任务状态 | 是从零开始还是在现有基础上扩展？ | ①从零开始 ②在现有文档基础上修改 ③补充已有方案的某个模块 |
| P3（影响细节） | 角色专业背景 | 需要引用哪些行业规范或专业标准？ | ①有具体规范（请说明） ②无规范要求 ③不确定 |
| P3（影响细节） | 遇歧义的处理方式 | 执行中遇到不明确的地方怎么处理？ | ①暂停并向我提问 ②自行判断并说明假设 ③标注存疑后继续 |

#### 2.2 逐题问答规则

**核心规则：一次只问一个问题，等用户回答后再问下一个。**

执行步骤：
1. 从 P1 维度开始，取出第一个缺失的问题
2. 用以下格式输出单个问题：

```
**问题 [N/总数]：[问题内容]**

可选答案：
A. [选项1]
B. [选项2]
C. [选项3]
D. 其他（请直接输入你的答案）

> 💡 此问题影响：[说明这个答案会影响提示词的哪个部分]
```

3. 等待用户回答（选择字母 或 直接输入文字 均有效）
4. 收到回答后，简短确认（一句话），然后提出下一个问题
5. 所有 P1、P2 问题回答完毕后，判断 P3 问题是否必要：
   - 若不影响提示词主体结构，可跳过 P3，直接进入 Step 3
   - 若 P3 问题影响关键设计，继续逐题提问

#### 2.3 问答结束标志

所有必要问题回答完毕后，输出一行摘要确认：

```
✅ 信息收集完毕，已确认：[列出关键信息的一句话摘要]
即将输出优化后的提示词……
```

然后进入 Step 3。

#### 2.4 可直接跳过提问的情况

以下情况无需提问，直接进入 Step 3：
- 原始提示词已包含角色、任务、格式、受众四个核心维度
- 用户在触发 `/prompt-optimizer` 时已附带了足够上下文（如本次对话已有问答记录）
- 原始提示词是简单单一任务（无需分层执行）

### Step 3：输出结构化提示词

使用以下模板结构输出优化后的提示词：

```
# 角色定义
[明确角色名称、专业背景、核心能力边界]
[如有行业规范，列出参考标准]

---

# 核心目标
[一句话说明最终目标，强调核心约束（如预算限制、效益最大化等）]

---

# 任务拆解（按优先级/执行顺序）

## 阶段一：[阶段名称]（先完成，再进入下一阶段）
1. [子任务1]
   - 输出形式：[具体格式]
   - 说明：[必要的补充说明]

2. [子任务2]
   ...

## 阶段二：[阶段名称]
...

---

# 输出规范
- [格式要求1]
- [格式要求2]
- 语言：[中文 / 英文 / 双语]

---

# 工作方式与约束
- [遇到歧义时的处理方式]
- [完成每阶段后的确认机制]
- [其他约束条件]

---

# 当前状态（由用户填写）
- [关键变量1]：[待填写]
- [关键变量2]：[待填写]
```

### Step 4：输出待确认问题清单（可选）

如果 Step 2 逐题问答后仍有 P3 级细节问题未确认，在提示词末尾追加「执行前待确认项」，
**不再重复已在 Step 2 中问过的问题**：

```
## 执行前可选确认项（不影响主体结构，按需回答）

**问题1（影响输出细节）**：[问题内容]
可选：A.[选项1]  B.[选项2]  C.其他

**问题2（影响边界条件）**：[问题内容]
可选：A.[选项1]  B.[选项2]  C.其他
```

如果 Step 2 已覆盖所有必要信息，此步骤可跳过。

---

## 输出质量标准

优化后的提示词应满足：

- ✅ Claude 读完后无需猜测就能开始执行
- ✅ 任务有明确的执行顺序和阶段划分
- ✅ 每个子任务有明确的输出格式
- ✅ 遇到歧义有明确的处理机制（提问 vs 假设）
- ✅ 角色背景与任务要求匹配
- ✅ 约束条件清晰（预算/时间/受众/边界）

---

## 典型优化示例

### 原始提示词（差）
> 你是一个高速公路养护专家，参考公路养护决策技术规范，完善甘肃公路养护决策系统的prd文档，补充整体的功能架构图和业务流程图，再逐个模块完善业务流程图和时序图，用于指导产品经理设计产品。在这期间有模糊不清的问题时随时向我提问。

**问题诊断：**
- 角色背景笼统（"高速公路养护专家"没有说明核心决策逻辑和知识边界）
- 目标模糊（"完善"没有说明完善到什么程度）
- 任务顺序不清（架构图和流程图的关系、模块顺序未定义）
- 输出格式未说明（图用什么格式？文字说明如何组织？）
- PRD 现状未知（从零开始还是有初稿？）

**触发逐题问答示例（Step 2 执行过程）：**

```
**问题 1/3：这个 PRD 文档目前处于什么状态？**

可选答案：
A. 从零开始，尚无初稿
B. 已有初稿，需要在现有基础上补充完善
C. 其他（请直接说明）

> 💡 此问题影响：任务拆解的起点和工作量估算
```

用户回答「B」后：

```
✅ 已确认：在现有 PRD 初稿基础上补充完善。

**问题 2/3：功能架构图和业务流程图期望用什么格式输出？**

可选答案：
A. Mermaid 代码（可在 Claude 界面直接渲染）
B. 文字描述（由产品经理自行绘制）
C. 两者都要：先出 Mermaid，再附文字说明
D. 其他

> 💡 此问题影响：输出规范和 Claude 执行方式
```

……依此类推，直到关键信息全部确认后输出结构化提示词。

### 优化后（好）
参见本次对话中输出的结构化提示词，包含：角色定义（含规范引用）、核心目标（含约束）、任务拆解（含阶段划分）、输出规范（Mermaid语法）、工作方式（遇歧义必提问）、待确认问题清单。

---

## 注意事项

- **不要过度设计**：如果原始提示词只是简单任务，不需要套完整模板，按需取用
- **保留用户意图**：优化是结构化，不是替换用户的目标，核心意图必须保留
- **中文场景优先**：对于中文提示词，输出也用中文，除非用户明确要求英文
- **不假设，多提问**：对于领域专业内容（如行业规范、系统模块范围），宁可提问也不要自行假设
